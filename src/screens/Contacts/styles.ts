import { StyleSheet } from 'react-native';

export default StyleSheet.create({
	container: {
		backgroundColor: '#fff',
	},
	loading: {
		height: '100%',
		alignItems: 'center',
		justifyContent: 'center',
	},
});
